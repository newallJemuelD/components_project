import 'package:flutter/material.dart';

class NavigationBarsScreen extends StatelessWidget {
  const NavigationBarsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SizedBox(
        child: Center(
          child: Text('This is navigation bars section content'),
        ),
      ),
    );
  }
}
