import 'package:flutter/material.dart';

class LibrariesNameListScreen extends StatelessWidget {
  const LibrariesNameListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SizedBox(
        child: Center(
          child: Text('This is libraries / features name list screen content'),
        ),
      ),
    );
  }
}
